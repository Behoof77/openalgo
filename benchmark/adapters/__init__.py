"""Model adapters for the unified benchmark.

Each adapter wraps a model server's ModelManager read-only (no modification,
no retraining, no hyperparameter tuning) behind the BaseAdapter interface.
"""
